<!DOCTYPE html>
<html lang="en">
  <head>
    <title> New Trip </title>
  </head>
  <body>
    <h1>Create New Trip</h1>

    <form class="" action="/trips" method="post">

      <label for="">Duration:</label>
        <p><input type="integer" name="trip[duration]" value=""></p>

      <label for="">Start Date:</label>
        <p><input type="datetime-local" name="trip[start_date]" value=""></p>

      <label for="">Start Station:</label>
        <p><select name="trip[start_station]">
          <% @stations.each do |station| %>
            <option value="<%= station.id %>"><%= station.name %></option>
          <% end %>
        </select></p>

      <label for="">End Date:</label>
        <p><input type="datetime-local" name="trip[end_date]" value=""></p>

      <label for="">End Station:</label>
        <p><select name="trip[end_station]">
          <% @stations.each do |station| %>
            <option value="<%= station.id %>"><%= station.name %></option>
            <% end %>
          </select></p>

      <label for="">Bike ID:</label>
        <p><select name="trip[bike_id]">
          <% Bike.all.each do |bike| %>
          <option value="<%= bike.id %>"><%= bike.bike %></option>
          <% end %>
        </select></p>


      <label for="">Subscription Type:</label>
        <p><select name="trip[subscription_type]">
          <% Subscription.all.each do |subscription| %>
            <option value="<%= subscription.id %>"><%= subscription.subscription_type %></option>
            <% end %>
          </select></p>

      <label for="">Zip Code:</label>
        <p><select name="trip[zip_code]">
          <% ZipCode.all.each do |zipcode| %>
          <option value="<%= zipcode.id %>"><%= zipcode.zip_code %></option>
          <% end %>
        </select></p>

      <p><input type="submit" name="submit" value="Create New Trip" /></p>
    </form>
  </body>
</html>
