<!DOCTYPE html>
<html lang="en">
  <head>
    <title> <%= @station.name %> </title>
  </head>

  <h1><%= @station.name %></h1>

  <p><h3><a href="/stations/<%= @station.id %>/edit">Edit</a></h3><br/>
    <form action="/stations/<%= @station.id %>" method="post">
    <input type="hidden" name="_method" value="DELETE" />
    <input type="submit" value="Delete" />
    </form></p>

  <body>

    <div class="container">

      <table class="table">

        <thead>
          <th>Station ID #</th>
          <th>Dock Count</th>
          <th>City</th>
          <th>Installation Date</th>
        </thead>

        <tbody>
          <tr>
            <th scope="row>"><%= @station.id %></th>
              <td><%= @station.dock_count %></td>
              <td><%= @city.find(@station.city_id).city %></td>
              <td><%= @station.installation_date %></td>
        </tbody>
      </table>

      <table class="table">

        <thead>
          <th>Date with Highest Number of Trips</th>
          <th>Frequently Used ZipCode for users from this Station</th>
          <th>Frequently Used Bike from this Station</th>
        </thead>

        <tbody>
          <tr>
            <th scope="row>"><%= Trip.date_for_highest_number_of_trips_from(@station.station_id) %></th>
            <td><%= Trip.frequently_used_zip_code_for_users_from(@station.station_id) %></td>
            <td><%= Trip.frequently_used_bike_id_from(@station.station_id) %></td>
          </tbody>
        </table>

      <table class="table">

        <thead>
          <th>Number of Rides Starting at this Station</th>
          <th>Number of Rides Ending at this Station</th>
          <th>Frequent Destination from this Station</th>
          <th>Frequent Origination to this Station</th>
        </thead>

        <tbody>
          <tr>
            <th scope="row>"><%= Trip.trips_from_starting(@station.station_id) %></th>
              <td><%= Trip.trips_from_ending(@station.station_id) %></td>
              <td><%= Trip.most_frequent_destination_from(@station.station_id) %></td>
              <td><%= Trip.most_frequent_origin_for_rides_ending_at(@station.station_id) %></td>
        </tbody>
      </table>

    </div>
  </body>
</html>
